{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd chess-site && npm install && npm run build",
  "outputDirectory": "chess-site/.next",
  "installCommand": "echo 'Skipping root install; chess-site installs its own deps.'",
  "framework": "nextjs"
}
